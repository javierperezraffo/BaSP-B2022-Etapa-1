// Variables y Operadores
    // a. Crear dos variables numéricas y utilizar el operador suma para guardar el valor de la suma de ambos números
    // en una 3er variable.
    // b. Crear dos variables de tipo String y concatenarlas guardando el resultado en una 3er variable.
    // c. Crear dos variables de tipo String y sumar el largo de cada variable (cantidad de letras del string)
    // guardando el resultado de la suma en una 3er variable (utilizar length).
// a
var numA = 2;
var numB = 8;
var numSum = numA + numB;
// b
var textA = 'ja';
var textB = 'jaj';
var textJoin = textA + textB;
// c
var strA = 'Lately';
var strB = 'I don\'t sleep';
var strLength = strA.length + strB.length;